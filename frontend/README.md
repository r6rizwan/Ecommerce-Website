# Frontend

ShopSphere frontend built with React and Bootstrap.

## Scripts
- `npm start` runs the development server
- `npm run build` creates the production build
- `npm test` runs the test runner

Frontend runtime configuration is provided through `frontend/.env`.
